name: skipapp
on:
  push:
    branches: [ master ]
    tags: "[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch:
  pull_request:

jobs:
  call-workflow:
    uses: skiptools/actions/.github/workflows/skip-app.yml@v1
